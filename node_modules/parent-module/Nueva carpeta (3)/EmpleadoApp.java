
package mintic.edu.upb.interfaz;

import java.util.Scanner;
import javax.swing.JOptionPane;
import mintic.edu.upb.mundo.Empleado;

/**
 * Clase de ejecución del proyecto main
 * @author Carlos Beltrán
 */
public class EmpleadoApp {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        // 1. Entrada
        // 1.1 Declarar e inicializar las variables a usar
        String inombre;
        int iedad;
        double isalario, iincremento, iSalarioNeto;
        // 1.2 Capturar el valor de los datos escritos por el usuario
        // 1.2.1 Definir un objeto tipo Scanner para capturar datos por consola
//    
//        Scanner sc = new Scanner(System.in);
//        // Por cada dato a capturar, un mensaje, se captura
//        System.out.print("Escriba el nombre del Empleado: ");
//        inombre = sc.nextLine();
//        System.out.print("Escriba la edad en años del Empleado: ");
//        iedad = sc.nextInt();
//        System.out.print("Escriba el salario del Empleado: ");
//        isalario = sc.nextDouble();
//    
        // Crear un objeto tipo Empleado
        // TipodeDatos nombrevariable = new Constructor
        inombre = JOptionPane.showInputDialog("Por favor escriba el nombre de la Persona: ");
        iedad = Integer.parseInt(JOptionPane.showInputDialog("Por favor escriba la edad de la Persona: "));
        isalario = Double.parseDouble(JOptionPane.showInputDialog("Por favor escriba el salrio de la Persona: "));
//        Empleado emp = new Empleado();
//        // Si el constructor es vacío tengo setear las variables
//        emp.setNombre(inombre);
//        emp.setEdad(iedad);
//        emp.setSalario(isalario);

        Empleado emp = new Empleado(inombre, iedad, isalario);
        double salarioNeto = emp.calcularSalarioNeto();
        JOptionPane.showMessageDialog(null, emp.calcularSalarioNeto());
//        System.out.println("El valor del Salario Neto es: "+salarioNeto);
        // 2. Proceso
        if (emp.esMenorDeEdad()){
            JOptionPane.showMessageDialog(null,"El empleado es Menor de Edad","Empleado Menor de Edad",JOptionPane.INFORMATION_MESSAGE);
//            System.out.println("El empleado "+inombre+" Es menor de Edad. No se manda solo");
        } else{
            JOptionPane.showMessageDialog(null,emp.toString());// 3.Salida
        }
    }
    
}
